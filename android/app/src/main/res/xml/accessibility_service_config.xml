<?xml version="1.0" encoding="utf-8"?>
<!--
  Config for SkipAccessibilityService.

  packageNames scopes the service to Skip so we don't receive events from every
  app on the phone (better for battery and privacy). Remove the attribute to
  observe all packages while debugging selectors.
-->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged|typeViewScrolled"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagReportViewIds|flagRequestFilterKeyEvents|flagDefault"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100"
    android:packageNames="com.delco.courier"
    android:description="@string/accessibility_service_description"
    android:settingsActivity="com.smartcourier.MainActivity" />
